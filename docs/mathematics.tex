\chapter{数学速查：条件先于公式}
\section{整除、卷积与筛法}\index{Dirichlet convolution}\index{Mobius inversion}
对正整数定义狄利克雷卷积
\[
(f*g)(n)=\sum_{d\mid n}f(d)g(n/d).
\]
单位元为 $\varepsilon(n)=[n=1]$，常函数 $1(n)=1$，恒等函数 $\operatorname{id}(n)=n$。
关键恒等式是 $\mu*1=\varepsilon$、$\varphi*1=\operatorname{id}$。
若 $F(n)=\sum_{d\mid n}f(d)$，则 $f(n)=\sum_{d\mid n}\mu(d)F(n/d)$。
\[
\sum_{i=1}^{a}\sum_{j=1}^{b}[\gcd(i,j)=1]
=\sum_{d=1}^{\min(a,b)}\mu(d)\lfloor a/d\rfloor\lfloor b/d\rfloor.
\]
整除分块：当左端点为 $l$、商为 $q=\lfloor n/l\rfloor$，右端点为 $r=\lfloor n/q\rfloor$。
同时包含多个商时，取各自右端点的最小值。
杜教筛由卷积恒等式获得
\[
M(n)=1-\sum_{l=2}^{n}M(\lfloor n/l\rfloor),\qquad
\Phi(n)=\frac{n(n+1)}2-\sum_{l=2}^{n}\Phi(\lfloor n/l\rfloor),
\]
求和按等商区间合并；第二式求和中没有额外乘以 $l$。

\section{杜教筛：预筛与缓存都不可省略}\index{Du Jiao sieve}
前节的两条卷积恒等式给出 \texttt{DuJiao} 的递推，见代码中的
\texttt{mertens} 与 \texttt{totient\_sum}。
把商相同的一段合并，系数为区间长度，递归参数严格变小；0 直接查表返回 0。
超过预筛上限的结果在返回时写入缓存，后续相同参数直接复用。
WIDA 收集的 \texttt{sumMu} 示例查询了 \texttt{fMu}，但未写入新结果；
本库已有实现包含写回，不能照搬遗漏缓存的版本。

设最大查询为 $N$，预筛上限为 $B$。单次查询的常用上界为
$O(B+N/\sqrt B)$，其中哈希访问按期望常数处理；取 $B$ 约 $N^{2/3}$
得到 $O(N^{2/3})$。多个不同查询可能继续增加缓存，不能把单次空间估计当成任意多询问的上界。
保留缓存可复用，主动清空仅影响重算开销；预筛表与上限应视为只读。

接口范围为 $0\le n\le10^{11}$，返回精确整数；欧拉函数前缀和用 128 位。
WIDA 的 \texttt{Z} 模数结果可在精确莫比乌斯前缀和返回后取模得到。
负值先求余，仅在余数为负时加模数，避免无条件加模数的溢出风险。

两套实现以独立试除构建到 200000 的前缀参考，检查四种预筛上限、零输入、
乱序与重复查询、缓存写回及清空重建。
上限 $10^{11}$ 使用 $10^6$ 与 $2\times10^6$ 两种预筛规模核对双版本结果；
这一项属于边界运行与一致性证据，不是独立答案对拍。
普通与 ASan/UBSan 均通过，暂无该接口的在线 AC。

\section{64 位判素与质因数分解}\index{Miller--Rabin}\index{Pollard--Rho}
本库判素采用七个固定底数的 Miller--Rabin，覆盖无符号 64 位整数：
\[
2,\ 325,\ 9375,\ 28178,\ 450775,\ 9780504,\ 1795265022.
\]
将 $n-1=d2^s$、$d$ 为奇数，检查 $a^d$ 及后续平方链。
小于 2 的输入不视为素数；底数模 $n$ 为 0 时跳过该底数，不可误判为合数。
模乘用无符号 128 位容纳完整的两个 64 位数之积。
固定底数的适用范围依据公开 SPRP 记录，不外推到任意精度整数。

\url{https://miller-rabin.appspot.com/}

Pollard--Rho 用 $f(x)=x^2+c\pmod n$ 迭代，并求 $\gcd(|x-y|,n)$。
只有 $1<d<n$ 的因子才返回；一次尝试超过轮数上限或得到 $d=n$，就换参数重试。
其中模平方后的加法也在 128 位中执行，避免 $n$ 接近 $2^{64}$ 时加 $c$ 溢出。
该过程保证返回真因子，但随机尝试次数没有确定的最坏上界。
递归分解两侧，直到判为素数，最后排序并保留重复因子。

调用 \texttt{factor(1)} 得到空数组；对 $n\ge2$，返回单元素说明 $n$ 为素数。
POJ 1811 所需的最小质因子可取排序结果首项；P4718 所需的最大质因子可取末项。
不要对空数组取首尾，也不要绕过 \texttt{factor} 把素数传给内部 \texttt{rho}。

双版本对百万范围以独立埃氏筛核验判素，对 $1\ldots10000$ 以试除核验完整因子列表。
另验证三个随机种子下的大质数平方、重复因子、强伪素数和 $2^{64}-1$；
期望因子的素性均以试除独立验证。
$2^{61}-1$ 使用完整分解 $n-1$ 的 Lucas 素性证书检查；模乘与模幂另以任意精度运算对拍。
完整 POJ 1811 与 P4718 驱动分别验证 350 组输入，检查素数标签与最小、最大质因子。
P4718 压力批次含 300 个接近 $10^{18}$ 的平衡半素数，本机普通编译运行约 1.99、1.83 秒；
机器差异与随机路径都会影响耗时，本地运行时间不直接代表线上性能。
两套代码随后通过 P4718 全部 20 个测试点，最长单点显示约 2.11、2.12 秒，低于当前页面的 4 秒限制。
该 AC 检查素数标签与最大质因子，范围为 $2\le n\le10^{18}$。
完整因子重数、最小质因子、$n=1$ 与更大的无符号 64 位输入仍依据本地验证；
普通与 ASan/UBSan 均通过，不构成随机运行时间保证。

来源：kuangbin 2.6；固定底数记录见上方链接。

\section{单个数的欧拉函数}\index{Euler totient}
实现见第~\pageref{compact-euler_phi}~页。对 $n\ge1$，由容斥得到
\[
\varphi(n)=n\prod_{p\mid n}\left(1-\frac1p\right),
\]
其中只枚举不同质因子，故质数幂 $p^k$ 的贡献为 $p^{k-1}(p-1)$。
每次更新按先除后乘的顺序：$\mathrm{ans}\gets(\mathrm{ans}/p)(p-1)$。
当前尚未处理的质因子仍整除答案，而且答案只会减小，乘法结果不会超过原始 $n$。

无分解器参数的入口采用试除，循环上界用 $p\le n/p$，剩下的 $n>1$ 再处理一次。
这能避免 $p^2$ 溢出，但不消除 $O(\sqrt n)$ 的耗时；不适合试除 64 位大素数。
传入现有 \texttt{PollardRho} 对象时先分解，再在排序后的因子列表上去重更新；
不重复构造分解器，后续调用沿用其随机状态。该入口没有确定的最坏运行时间。
$\varphi(1)=1$，两个入口均接受完整的正无符号 64 位输入；0 不在定义域内。

本地双版本验证包含 $1\ldots20000$ 的线性筛交叉检查、小范围直接计数互质整数，
以及重复质因子、大质数平方、$2^{64}-1$ 和已独立认证的大素数。
普通与 ASan/UBSan 测试均通过；P4718 的 AC 只验证底层分解用途，不是此欧拉函数的新 AC。
来源：kuangbin 2.7.1、2.7.3；WIDA 的单个数欧拉函数。

\section{线性筛：三张表的约定}\index{linear sieve}
\texttt{LinearSieve} 同时构造最小质因子、欧拉函数和莫比乌斯函数。
内层遇到第一个整除 $i$ 的素数后停止，因此每个合数 $j$ 只由
$p=\operatorname{lp}(j)$ 与 $i=j/p$ 生成一次，时间为 $O(n)$。
若 $p\mid i$，则 $\varphi(ip)=p\varphi(i)$、$\mu(ip)=0$；
否则二者分别为 $(p-1)\varphi(i)$ 和 $-\mu(i)$。

判素使用 $x\ge2$ 且 $\operatorname{lp}(x)=x$，不能漏掉前半条件。
分解表内正整数时，反复取 $p=\operatorname{lp}(x)$、令 $x\gets x/p$，直到 $x=1$；
重复因子会重复输出。0 不可分解，1 的质因子列表为空。
上限要求 $0\le n<\texttt{INT\_MAX}$，避免构造时 $n+1$ 溢出，并保证内存可用。
筛乘积前先检查 $p\le n/i$，不直接计算可能溢出的 $ip$。

双版本验证至 $n=10^7$，包括小前缀、试除最小质因子、因子还原和逐项约数和证书：
\[
\sum_{d\mid x}\varphi(d)=x,\qquad\sum_{d\mid x}\mu(d)=[x=1].
\]
这些恒等式在 $d=x$ 项系数为 1，可按 $x$ 递增唯一确定两张表。
普通与 ASan/UBSan 均通过；不据此宣称莫比乌斯反演的所有应用已验证。

来源：kuangbin 2.1.1、2.1.2、2.7.2、2.7.4、2.13.1；WIDA 的全部欧拉函数表。

WIDA 的“使用莫比乌斯反演求解欧拉函数”先存储全部约数，再使用
\[
\varphi(n)=n-\sum_{d\mid n,\ d<n}\varphi(d).
\]
这与 $\varphi*1=\operatorname{id}$ 等价；再卷积 $\mu$ 可得
$\varphi(n)=\sum_{d\mid n}\mu(d)(n/d)$。
本库用线性筛提供同一张表，无需额外保存约数列表。
百万范围另分别运行直接卷积和真约数扣减，逐项核对两套筛表；两种方法都不复用线性筛的递推分支。


\section{区间筛：只保存目标区间}\index{segmented sieve}\index{区间筛素数}
实现见第~\pageref{compact-segmented_primes}~页。
设 $0\le L\le R\le\texttt{INT\_MAX}$，先筛出不超过 $\lfloor\sqrt R\rfloor$ 的素数，
再对每个素数 $p$ 从
\[
\max\bigl(p^2,\lceil L/p\rceil p\bigr)
\]
开始标记区间内的倍数。任何合数都有不超过自身平方根的素因子，所以不会遗漏；
从 $p^2$ 开始也不会误删 $p$ 自身。输出跳过 0 和 1，其余未标记数恰为素数。
底筛与区间标记共用同一个外层素数循环，但使用两张独立的表。

令 $W=R-L+1$，空间为 $O(\sqrt R+W)$，包含返回的素数数组；
时间为 $O((\sqrt R+W)\log\log(R+3))$。函数每次调用都重建底筛，适合题目中的少量区间查询。
端点虽然是 \texttt{int}，向上取整的加法、$p^2$、标记变量和输出遍历变量均用
\texttt{long long}；否则 $R=\texttt{INT\_MAX}$ 时最后一次递增也会溢出。
区间长度仍受可用内存约束，不适合一次保存整个 32 位值域。

POJ 2689 与 UVa 10140 的驱动读到 EOF，扫描相邻素数差。
最近、最远都只在严格更优时更新，距离并列保留最先出现的一对；少于两个素数时输出题目指定句子。
两套实现以试除和 Miller--Rabin 判素进行独立对拍，并检查平方边界、单点区间及
右端点为 \texttt{INT\_MAX} 的百万长度区间；完整驱动验证 208 组输入，含官方样例。
普通测试与 ASan/UBSan 均通过；尚无本库在线 AC。

来源：kuangbin 2.1.3；\url{https://onlinejudge.org/external/101/10140.pdf}。

\section{矩形 GCD 计数：缩放后容斥}\index{coprime pairs}\index{矩形 GCD 计数}
实现见第~\pageref{compact-CoprimePairs}~页。
由 $[g=1]=\sum_{d\mid g}\mu(d)$，前缀矩形互质数对数为
\[
F(A,B)=\sum_{i=1}^{\min(A,B)}\mu(i)\left\lfloor\frac Ai\right\rfloor
\left\lfloor\frac Bi\right\rfloor.
\]
两个商同时不变的右端点取 $r=\min(A/\lfloor A/l\rfloor,B/\lfloor B/l\rfloor)$，
用莫比乌斯前缀和合并 $[l,r]$。不能只按一个商分块。
只需筛到 $\min(A,B)$，不要求较长的一边也在预处理范围内。
$A=0$ 或 $B=0$ 时循环为空，返回 0；正整数条件不包含坐标轴。

矩形内 $\gcd(x,y)=k$ 等价于 $x,y$ 均为 $k$ 的倍数，且缩放后互质。
因此答案为四个前缀矩形的容斥：
\[
\begin{aligned}
&F(\lfloor b/k\rfloor,\lfloor d/k\rfloor)
-F(\lfloor(a-1)/k\rfloor,\lfloor d/k\rfloor)\\
&-F(\lfloor b/k\rfloor,\lfloor(c-1)/k\rfloor)
+F(\lfloor(a-1)/k\rfloor,\lfloor(c-1)/k\rfloor).
\end{aligned}
\]
这里必须先减 1 再除以 $k$；零商无需特殊补偿。
输入端点为正 \texttt{int}，最终答案不超过矩形面积，能用有符号 64 位保存。
分块累加使用 128 位，避免依赖正负中间项的额外界。

双版本以小矩形逐对求 gcd 为独立参考，另验证百万正方形的
$F(N,N)=2\sum_{i=1}^N\varphi(i)-1$，以及按短边剩余类直接计数的 \texttt{INT\_MAX} 长条。
P2522 双版本完整程序验证五万次查询，包括缩放、窄边和无解；普通与 ASan/UBSan 均通过。
尚无本库在线 AC。

来源：kuangbin 2.13.2（BZOJ 2301）；\url{https://www.luogu.com.cn/problem/P2522}。

\section{整数分拆：重数限制的差一问题}\index{integer partitions}\index{pentagonal number theorem}
实现见第~\pageref{compact-Partitions}~页。分拆不区分加数顺序，部分均为正整数。
无约束生成函数 $P(z)=\prod_{i\ge1}(1-z^i)^{-1}$，空分拆给出 $p(0)=1$。
五边形数定理为
\[
\prod_{i\ge1}(1-z^i)=1+\sum_{j\ge1}(-1)^j
\left(z^{j(3j-1)/2}+z^{j(3j+1)/2}\right).
\]
与 $P(z)$ 相乘后比较系数，得到 $p(n)$ 的递推，符号按 $+,+,-,-,+,+,\ldots$ 成对出现。
每项只依赖更小的下标，每个 $n$ 有 $O(\sqrt n)$ 个非零偏移。
实现先用 64 位累加，再归一化到模数范围；无需逆元，合数模数与模数 1 均可使用。

每个正整数最多出现 $k$ 次的生成函数是
\[
\prod_{i\ge1}(1+z^i+\cdots+z^{ki})
=P(z)\prod_{i\ge1}(1-z^{(k+1)i}).
\]
因此用已有 $p$ 表与放大 $k+1$ 倍的五边形偏移相乘，符号为 $-,-,+,+,\ldots$。
本库 \texttt{limited(n,k)} 的上限包含 $k$；$k=0$ 时非零 $n$ 无解，$k=1$ 为互异分拆。
kuangbin 的注释写“不超过 k 个”，但其 \texttt{solve(n,k)} 使用放大 $k$ 倍的公式，
实际表示每种数少于 $k$ 次，对应本库 \texttt{limited(n,k-1)}（$k\ge1$）。
HDU 原题页面本次无法读取，不能由这条注释推断原题措辞。

两套实现以任意精度完全背包验证到 1000，以逐种部分、逐个重数枚举验证有界情况到 100，
覆盖素数与合数模数、0/1 重数限制和大于总和的限制；另检查 $N=100000$ 的边界。
普通与 ASan/UBSan 均通过，暂无在线 AC。

来源：kuangbin 2.11；\url{https://oi-wiki.org/math/combinatorics/partition/}。

\section{幂的约数和：不除以 $p-1$}\index{divisor sum}\index{geometric series}
实现见第~\pageref{compact-divisor_sum_power}~页。
若 $a=\prod p_i^{e_i}$，则 $a^b$ 的约数和是
$\prod_i(1+p_i+\cdots+p_i^{e_i b})$。
几何级数的除法公式在模意义下要求分母可逆；合数模数或 $p\equiv1$ 时不能直接套用。

令 $G(a,n)$ 同时返回 $P=a^n$ 与 $S=\sum_{i=0}^{n-1}a^i$。
先递归求一半项数 $h=\lfloor n/2\rfloor$ 的 $(P,S)$；
偶数项的结果为 $(P^2,S(1+P))$，奇数项再补最后的 $a^{2h}=P^2$，
得到 $(P^2a,S(1+P)+P^2)$，全过程取模。
$n=0$ 的结果为 $(1,0)$，因此底数为 0 时，非空级数的和仍为 1。
本接口参数是项数，不是最高指数；约数和必须传 $e_i b+1$。

把幂与和一起返回，可使每层只做常数次运算，总时间 $O(\log(n+1))$。
长度允许无符号 128 位，避免 $e_i b$ 在 64 位中溢出；模数及底数允许完整无符号 64 位。
所有模乘与必要的加法在 128 位中执行，任意正模数均可用。
底数 $a\ge1$，$b=0$ 或 $a=1$ 都返回 $1\bmod m$；0 的约数和不在接口定义域内。

双版本以直接枚举约数、逐项几何求和和任意精度矩阵幂独立核验，
包括模数 1、合数模数、完整 128 位项数，以及指数乘重数超过 64 位的案例。
P1593 完整程序各验证 126 组输入，包含 $p\equiv0,1\pmod{9901}$ 和最大输入；
普通与 ASan/UBSan 均通过，在线 AC 待补。

来源：kuangbin 2.12；\url{https://www.luogu.com.cn/problem/P1593}。

\section{类欧几里德：有符号整除求和}\index{floor sum}\index{类欧几里德}
\texttt{NumberTheory::floor\_sum(n,m,a,b)} 返回
\[
F(n,m,a,b)=\sum_{i=0}^{n-1}\left\lfloor\frac{ai+b}{m}\right\rfloor.
\]
传统接口为 \texttt{Number\_Theory::Floor\_Sum}。
范围约定 $0\le n\le10^9$、$1\le m\le10^9$，$a,b$ 支持完整有符号 64 位，返回 128 位。
$n$ 是项数；若题目下标为 $0\ldots N$，需传 $N+1$ 并检查仍在约定范围内。

先令 $a=q_a m+r_a$、$b=q_bm+r_b$，其中余数属于 $[0,m)$，
答案先加上 $q_a n(n-1)/2+q_b n$。
C++ 的负整数除法向零截断，因此余数为负时需要把商减 1，再用 128 位乘法计算规范余数。
这一步不能简单用两次取模替代后丢弃商的贡献。

对规范化后的 $0\le a,b<m$，设 $y=an+b$。
若 $y<m$，余下贡献为 0；否则交换格点计数的求和方向可得
\[
F(n,m,a,b)=F(\lfloor y/m\rfloor,a,m,y\bmod m).
\]
下一轮先提取新的整商，再继续交换。进入交换分支时 $a>0$，不会产生零分母。
模数按欧几里得过程减小，时间 $O(\log(m+1))$，额外空间 $O(1)$。

双版本以任意精度向下取整逐项求和对拍，覆盖负系数、零项数和整除边界；
十亿项的常数、连续整数、反向连续整数及整倍数斜率使用闭式公式独立核验。
同时检查模数 1 下两个系数的 64 位极值。普通与 ASan/UBSan 均通过，暂无在线 AC。
本节只验证基本整除和；下节单独给出带下标权与平方和的接口及证据。

\section{类欧几里德的三个求和量}\index{floor moments}
实现见第~\pageref{compact-floor_moments}~页。令 $y_i=\lfloor(ai+b)/m\rfloor$，
维护 $f=\sum y_i$、$g=\sum i y_i$、$h=\sum y_i^2$，下标均为 $0\le i<n$。
结果固定模 $998244353$，不要更换模数却保留 2、6 的逆元常数。

先把 $a=xm+a'$、$b=ym+b'$ 归一化；设规范化问题返回 $(f_0,g_0,h_0)$，
并记 $S_1=\sum i$、$S_2=\sum i^2$，则
\[
\begin{aligned}
f&=f_0+xS_1+yn,\\
g&=g_0+xS_2+yS_1,\\
h&=h_0+x^2S_2+y^2n+2xyS_1+2xg_0+2yf_0.
\end{aligned}
\]
负系数的商仍须向下取整。规范化后的 $a'=0$ 时三个剩余量均为 0。

对 $0<a'<m$，令 $t=\lfloor(a'(n-1)+b')/m\rfloor$。
高度 $j=1\ldots t$ 首次出现的下标为 $r_j=\lceil(mj-b')/a'\rceil$。
以 $q=j-1$ 编号，递归参数为 $(t,a',m,m+a'-b'-1)$；设返回 $(u,v,w)$，即
$u=\sum r_j$、$v=\sum(j-1)r_j$、$w=\sum r_j^2$，则
\[
f_0=nt-u,\qquad g_0=tS_1-(w-u)/2,\qquad h_0=nt^2-2v-u.
\]
这来自按高度计算每一层的贡献，其中 $y_i^2=\sum_{j=1}^{y_i}(2j-1)$。
分母每次严格减小，所以只有 $O(\log(m+1))$ 层递归。

P5170 的上界包含 $n$，驱动传入 $n+1$；题目输出顺序为 $f,h,g$，与本接口不同。
核心函数支持十亿加一项，并对完整有符号 64 位系数做任意精度逐项对拍；
大项数用一次多项式的和与平方和闭式检查。
两套完整程序各验证十万组输入，检查最大闭区间端点与全部三个输出。
普通与 ASan/UBSan 均通过，在线 AC 待补；任意更高次矩尚不在这个接口内。

补充的大参数测试包含 3000 组随机参数、64 位系数端点和斐波那契型欧几里得链。
反转 $i\mapsto n-1-i$ 应保持 $f,h$ 不变，并使 $g$ 变为 $(n-1)f-g$。
将每项替换为 $-y_i-1$，三个结果应变为 $-f-n$、$-g-S_1$、$h+2f+n$。
相邻前缀还应满足增量 $(y_n,ny_n,y_n^2)$，其中新增项以任意精度计算。
第一结果另与独立接口 \texttt{floor\_sum} 交叉核对。
这些恒等式测试是大参数一致性证据，与前述逐项独立答案对拍区分记录；普通与 ASan/UBSan 均通过。

来源与题目：\url{https://www.luogu.com.cn/problem/P5170}。

\section{模运算的适用条件}\index{CRT}\index{Euler theorem}\index{Lucas theorem}
模逆元存在当且仅当 $\gcd(a,m)=1$。费马逆元 $a^{p-2}$ 仅适用于素数模数及 $a\not\equiv0$。
对正模数 $m,n$，同余式 $x\equiv a\pmod m$、$x\equiv b\pmod n$ 可合并当且仅当
$\gcd(m,n)\mid(b-a)$；新模数为 $\operatorname{lcm}(m,n)$。
\[
\binom nk\equiv\prod_i\binom{n_i}{k_i}\pmod p\quad(p\text{ 为素数，}n_i,k_i\text{ 是 }p\text{ 进制位}).
\]
Lucas 不能直接套在合数模数。阶乘与逆阶乘预处理要求上限小于模数；超过模数会出现阶乘为零。
扩展欧拉降幂中，非互质情况不能直接将指数对 $\varphi(m)$ 取模；需要判断原指数是否达到足够阈值。
大整数指数可以在读入时同时维护余数与是否超过阈值。

\section{任意数组的批量逆元}\index{批量逆元}
设 $s_i=\prod_{j<i}a_j\pmod m$，$s_0=1$。
仅对总乘积 $s_n$ 求一次逆元，随后从右向左处理。
若当前 $t=s_{i+1}^{-1}$，则
\[
a_i^{-1}=t s_i\pmod m,\qquad s_i^{-1}=t a_i\pmod m.
\]
先保存原 $a_i$ 再覆盖输出，避免误用已写入的逆元更新 $t$。
总乘积与 $m$ 互素当且仅当每一项均与 $m$ 互素，
因此一次扩展欧几里得也能判断整个批次是否可行；模数无需为素数。
实现见第~\pageref{compact-batch_inverse}~页。

接口返回可选向量：无解是空的可选值，空输入是有效的空向量，两者不同。
模数为 1 时按接口约定返回全零。调用者可以移动不再使用的输入数组，避免额外复制。
P5431 的驱动程序计算 $\sum_{i=1}^n k^i/a_i$，注意权重从 $k^1$ 开始。
五百万项规模采用缓冲的字符读取，并移动输入以控制峰值内存。
双版本已做乘法证书和非互素拒绝的穷举检查，以及 64 位极值、空数组、模数 1 的检查；
完整驱动另用逐项逆元和几何级数进行对拍。P5431 双版本均通过全部 5 个测试点；
动态版记录 297561197，传统版记录 297561804。最慢单点分别为 386、384 毫秒，峰值内存均为 76.80 MB。
合数模数、不可逆输入和完整 64 位范围以上述本地测试验证。

该方法可处理任意可逆数组；连续整数的专用递推见下一节。

来源：\url{https://oi-wiki.org/math/number-theory/inverse/}。

\section{连续整数的线性逆元表}\index{线性逆元表}
素数模数 $p$ 下，对 $1<i<p$，写 $p=qi+r$，其中 $q=\lfloor p/i\rfloor$，$r=p\bmod i$。
$r$ 非零，且 $r<i$。将等式模 $p$ 后乘以 $i^{-1}r^{-1}$，得到
\[
i^{-1}\equiv-\lfloor p/i\rfloor\,(p\bmod i)^{-1}\pmod p.
\]
从 $1^{-1}=1$ 开始递推，右侧总是已算过的较小下标，故总时间为 $O(N)$。
实现见第~\pageref{compact-inverse_table}~页；第 0 项只作占位，允许 $N=0$。
$N\ge p$ 时区间包含无逆元的 $p$，不能套用该表。

递推还依赖 $r$ 可逆，不能仅凭 $i$ 与合数模数互素就照搬。
例如 $i=3,m=8$ 时 $r=2$ 不可逆。kuangbin 的“简洁写法”递归式也有这一限制，
本书以一般扩展欧几里得接口代替其不成立的合数推广。
若 $\gcd(a,m)=1$，扩展欧几里得给出 $ax+my=1$，归一化 $x$ 即为逆元；
若不互素，则整数方程无解。
素数下也可用 $a^{p-2}$ 求逆元；kuangbin 标为“利用欧拉函数”的代码实际使用这一费马形式。

P3811 双版本完整程序逐行输出逆元，已在本地对三百万行结果逐项检验 $ix_i\equiv1$。
P3811 双版本均通过全部 6 个测试点，单点最长分别为 221 ms 与 219 ms。
单个逆元另用小模数穷举与费马乘法证书交叉验证；这部分不由 P3811 的 AC 覆盖。

来源：\url{https://oi-wiki.org/math/number-theory/inverse/}；WIDA 数论逆元节与 kuangbin 2.4。

\section{一次不定方程：特解与正整数解范围}\index{Bezout identity}\index{二元一次不定方程}
实现见第~\pageref{compact-linear_equation}~页。
对不全为零的 $a,b$，令 $g=\gcd(|a|,|b|)>0$。
递归扩展欧几里得先求 $|a|u+|b|v=g$；当且仅当 $g\mid c$ 时有整数解。
把系数乘以 $c/g$，再恢复 $a,b$ 的符号，就得到 $ax_0+by_0=c$。
两个特解相减，再利用 $a/g,b/g$ 互素，可知所有解恰为
\[
x=x_0+\frac bg t,\qquad y=y_0-\frac ag t,\qquad t\in\mathbb Z.
\]
若 $a=b=0$，则 $c=0$ 时所有整数对都是解，否则无解；此时不能使用除以 $g$ 的通解式。

底层 \texttt{exgcd} 的本库调用约定为非负系数，上限 $2^{63}$；
返回非负的 $g$，$(0,0)$ 约定返回 0。
包装函数先转为 128 位再取相反数，因而可以处理 \texttt{LLONG\_MIN}。
缩放后的特解也用 128 位保存；不要提前转回 \texttt{long long}。
即使特解可表示，直接验证 $ax+by$ 的两个乘积仍可能超过 128 位，本地验证使用任意精度整数。

对 P5656 的正系数 $a,b,c$，设 $d_x=b/g,d_y=a/g$，
先将 $x_0$ 调整到 $x_{\min}\in[1,d_x]$，并计算与它配对的
$y_{\max}=(c-ax_{\min})/b$。
若 $y_{\max}>0$，正整数解个数为
\[
N=\left\lfloor\frac{y_{\max}-1}{d_y}\right\rfloor+1,
\quad x_{\max}=x_{\min}+(N-1)d_x,
\quad y_{\min}=y_{\max}-(N-1)d_y.
\]
否则不存在同时为正的解，但 $x$ 和 $y$ 仍各自有最小正值；
分别按周期归一化即可，它们通常不是同一组解。正值归一化使用
\texttt{(v \% d + d - 1) \% d + 1}，不要把余数 0 当作正整数。
驱动中 $a,b,c\le10^9$，归一化后的乘积不超过 $10^{18}$，故该部分可用 64 位。

双版本以小范围有符号输入穷举、任意精度等式证书、64 位端点和斐波那契递归链验证。
完整 P5656 驱动另验证 200000 组输入，包含正解数量与四个极值、无解和无正解分支；
普通与 ASan/UBSan 测试均通过，在线 AC 待补。

来源：kuangbin 2.3；WIDA 数论打印稿的 exgcd 与不定方程例题。

\url{https://oi-wiki.org/math/number-theory/bezouts/}。

\section{线性同余：最小解与最小周期}\index{linear congruence}\index{线性同余方程}
实现见第~\pageref{compact-linear_congruence}~页，接口约定为 $ax+b\equiv0\pmod m$。
先把 $a$ 归一化到 $[0,m)$，令 $g=\gcd(a,m)$。
有解当且仅当 $g\mid b$；若 $au+mv=g$，则一个特解为 $u(-b/g)$，
所有解为 $x=x_0+(m/g)t$。归一化到 $[0,m/g)$ 后得到最小非负解。
除以 $g$ 后 $a/g$ 与 $m/g$ 互素，因而 $m/g$ 是最小正周期。
当 $a=0$ 且 $m\mid b$ 时所有整数均为解，返回 $(0,1)$；否则无解。
模数 $m=1$ 始终返回 $(0,1)$。

输入常数项支持 \texttt{LLONG\_MIN}，内部用 \texttt{-i128(b)}，
不能先执行 64 位的 \texttt{-b} 再转换。
若题目给出 $ax\equiv c\pmod m$，可先将 $c$ 归一化到 $[0,m)$ 再取负传入，
避免 $c=\texttt{LLONG\_MIN}$ 时在调用点溢出。
本接口依赖的 exgcd 与前节相同，不必重复抄写。

P1516 中相遇条件为 $(m-n)t+(x-y)\equiv0\pmod L$。
传入速度差与起点差，第一返回值就是最早非负相遇时间；失败时输出 \texttt{Impossible}。
不能擅自把负的速度差取绝对值，也不能把两者同余的初始位置强制判为需要跳跃。

小模数测试逐个枚举全部剩余类，检查解集、最小解和最小周期；大数用任意精度等式证书验证。
完整驱动以直接模拟、交换青蛙和构造大周期最早相遇时间验证 242 组输入，
包括等速和初始位置同余。两套代码均通过普通与 ASan/UBSan 测试，在线 AC 待补。

来源：WIDA 收集的 jiangly exgcd 文件内 \texttt{sol}；
\url{https://www.luogu.com.cn/problem/P1516}。

\section{扩展 CRT：逐式合并}\index{exCRT}\index{扩展中国剩余定理}
kuangbin 2.5 的 \texttt{solve} 对应单次合并，\texttt{MLES} 对应下面的逐式循环。
实现见第~\pageref{compact-NumberTheory}~页的 \texttt{crt}，
传统版对应 \texttt{Crt}。初始取 $r=0,m=1$，依次合并 $x\equiv b\pmod n$。
空方程组保持 $(0,1)$，表示任意整数；一旦无解即停止，不能忽略失败继续合并。
先把余数归一化。设 $g=\gcd(m,n)$，由扩展欧几里得得到 $mu+nv=g$。
写 $x=r+mt$，原问题化为 $mt\equiv b-r\pmod n$。
若 $g\nmid(b-r)$ 则无解，否则
\[
q=n/g,\qquad t\equiv\frac{b-r}{g}u\pmod q,\qquad 0\le t<q.
\]
新周期为 $mq$，新余数为 $(r+mt)\bmod(mq)$。
所有可行 $t$ 相差 $q$，故新余数加新周期的任意整数倍恰好是全部解；
逐式合并时这一不变量保持成立。模数不必互素，模数 1 也可直接参与。

参考实现使用 \texttt{int} 保存合并状态，本库扩展为 64 位并显式报告周期越界；
避免可能溢出的 \texttt{abs(a-a0)}。

代码要求正模数且最终周期不超过有符号 64 位上限；
中间乘法与扩展欧几里得系数使用 128 位整数。
无解返回假；有解而周期越界抛出异常。失败时旧周期不变，旧余数已归一化。

$k$ 条方程的时间为 $O(k\log M)$，其中 $M$ 是输入及中间模数的上界；
递归欧几里得使用 $O(\log M)$ 栈空间。

P4777 的完整程序按“模数、余数”读入，题目保证有解且周期不超过 $10^{18}$。
本地验证包含双版本小模数穷举、任意精度整数对拍、有符号极值、无解与越界，
以及十万条同余式的完整程序测试。另穷举至多三个小模数方程组成的所有有序系统，检查空系统与后续矛盾。
双版本均已通过 P4777 全部 15 个测试点；
动态版记录 297521102，传统版记录 297521173。

kuangbin 的“解同余方程组”是模 7 的线性方程组消元应用，见后面的工厂工期一节。
来源：\url{https://oi-wiki.org/math/number-theory/crt/}；WIDA 数论模板的 excrt 节。

\section{Garner：总周期可以超过整数范围}\index{Garner}\index{混合进制}
设正模数 $m_i$ 两两互素，$P=\prod_i m_i$。
经典构造为 $x=\sum_i b_iP_i(P_i^{-1}\bmod m_i)\bmod P$，其中 $P_i=P/m_i$。
模 $m_i$ 时其他项都为零，本项为 $b_i$；两解之差被所有 $m_i$ 整除，因而被 $P$ 整除。
但直接写此式需要存储整个 $P$，并防止乘法溢出。

Garner 改用混合进制
\[
x=d_0+d_1m_0+d_2m_0m_1+\cdots,\qquad 0\le d_i<m_i.
\]
在确定第 $i$ 位前，对每个后续模数维护已确定部分的余数 $v_j$，
以及当前位权 $c_j=\prod_{t<i}m_t\bmod m_j$。于是
\[
d_i=(b_i-v_i)c_i^{-1}\bmod m_i.
\]
把 $c_jd_i$ 加到 $v_j$，再将 $c_j$ 乘 $m_i$ 即可进入下一位。
互素性保证逆元存在；模数 1 的位固定为 0，可直接保留。
位数范围保证重建的 $x$ 恰在 $[0,P)$，因此就是最小非负解。
实现见第~\pageref{compact-garner}~页。

若只需 $x\bmod T$，从高位向低位做 Horner 计算：每步将当前值乘对应模数，
加上该位，再对 $T$ 取模。$T$ 无需与 $m_i$ 互素，因为这里不对 $T$ 求逆。
这返回的是规范解 $x$ 的余数；一般解 $x+kP$ 对 $T$ 的余数可能不同。
例如模 3 余 2、模 5 余 3，规范解是 8；对 7 取模为 1，但另一个解 23 的余数是 2。
若用多模 NTT 恢复整数系数，仍须证明真实系数位于所选规范区间，
不能在乘积不够大时直接把 CRT 的代表元当成原整数。

双版本本地验证使用任意精度整数的经典 CRT 求和作为独立对拍，
检查混合进制的完整重建、负余数、有符号极值、非互素输入拒绝和 1000 个模数。
P1495 完整程序按题目保证的 $P\le10^{18}$ 输出规范解，双版本均通过全部 11 个测试点；
动态版记录 297521594，传统版记录 297521658。超出该题范围的能力以上述本地测试验证。
来源：\url{https://oi-wiki.org/math/number-theory/crt/}。

\section{素数模平方根}\index{Cipolla}\index{quadratic residue}
奇素数 $p$ 下，对 $n\not\equiv0\pmod p$，Euler 判别式
$n^{(p-1)/2}\equiv1\pmod p$ 当且仅当存在平方根，否则为 $-1$。
当 $p\equiv3\pmod4$ 且有解时，$r=n^{(p+1)/4}$ 为一根，另一根是 $p-r$。

Cipolla 选择 $a$ 使 $w=a^2-n$ 是非平方，令 $\omega^2=w$。
在扩域中取 $\alpha=a+\omega$，有 $\alpha^p=a-\omega$，故 $\alpha^{p+1}=n$。
令 $\beta=\alpha^{(p+1)/2}$，则 $\beta^2=n$ 且
$\beta^{p-1}=n^{(p-1)/2}=1$，所以 $\beta$ 落在原素数域中。
实现见第~\pageref{compact-mod_sqrt}~页；零和模数 2 单独处理。
模数为合数时不能照搬此判别和扩域构造。

\section{乘法阶与原根}\index{原根}\index{multiplicative order}
对 $\gcd(a,n)=1$，阶是最小的正整数 $d$ 使 $a^d\equiv1\pmod n$，且 $d\mid\varphi(n)$。
从 $d=\varphi(n)$ 开始，对每个素因子 $q$，只要 $a^{d/q}\equiv1$ 就继续除去 $q$，
所得即为实际阶。对于不可逆的 $a$，接口返回 0，表示此处没有定义乘法阶。

原根要求阶达到 $\varphi(n)$。只需对其每个不同素因子 $q$ 检查
$a^{\varphi(n)/q}\not\equiv1$：若实际阶是真因子，商中必含某个这样的 $q$。
必须先检查 $\gcd(a,n)=1$；例如模 9 的候选 3 虽通过上述非 1 判定，却不是原根。
$n\ge2$ 时，原根恰存在于 $2,4,p^k,2p^k$，其中 $p$ 是奇素数。
原根存在且取一个原根 $g$ 后，$g^t$ 的阶是
$\varphi(n)/\gcd(t,\varphi(n))$，所以枚举与 $\varphi(n)$ 互素的指数即可得到全部原根，
共 $\varphi(\varphi(n))$ 个；这些数按大小输出时还需排序。

实现见第~\pageref{compact-PrimitiveRoot}~页。
本地验证穷举小模数的幂循环，与双版本的阶、最小值和完整集合逐项比较；
另验证大素数、奇素数幂、无原根合数和有符号输入边界。
P6091 的完整程序输出排序后第 $d,2d,\ldots$ 个原根，零个输出时仍保留空行。
双版本均通过全部 11 个测试点；动态版记录 297558740，传统版记录 297559082。
求阶、最小原根的最小性及更大输入范围仍以上述本地证据验证。
若 $d=\operatorname{ord}_n(a)$，则 $\operatorname{ord}_n(a^k)=d/\gcd(d,k)$。
两个可逆元素乘积的阶整除各自阶的最小公倍数；两阶互素时，乘积的阶等于两阶之积。
来源：\url{https://oi-wiki.org/math/number-theory/primitive-root/}；kuangbin 2.17。

\section{Carmichael：最小公共指数}\index{Carmichael}\index{Korselt criterion}
$\lambda(n)$ 是模 $n$ 的所有可逆元素之阶的最小公倍数，约定 $\lambda(1)=1$。
它整除 $\varphi(n)$，但可以严格更小，例如 $\lambda(8)=2$ 而 $\varphi(8)=4$。
按互素的素数幂分解 $n=\prod_i p_i^{e_i}$ 后，由 CRT，
对每个分量同时成立的幂等式也对 $n$ 成立，因此
\[
\lambda(n)=\operatorname{lcm}_i\lambda(p_i^{e_i}),\qquad
\lambda(p^e)=
\begin{cases}
2^{e-2},&p=2,\ e\ge3,\\
p^{e-1}(p-1),&\text{其他情形}.
\end{cases}
\]
奇素数幂的单位群有原根，公共指数即其大小。
模 $2^e$ 的奇数可表示为 $\pm5^k$，当 $e\ge3$ 时 5 的阶为 $2^{e-2}$，
这给出二幂情形的上界和达到上界的元素。
实现见第~\pageref{compact-carmichael}~页，试除版本适用于有符号 32 位正模数。

对可逆底数可把指数对 $\lambda(n)$ 取模；非互素底数仍须另行处理。
例如模 8 下 $2^2=4$，但 $2^{2\bmod\lambda(8)}=1$，直接降幂会错误。
另有 $\lambda(\operatorname{lcm}(a,b))=\operatorname{lcm}(\lambda(a),\lambda(b))$，
这也提供了独立的组合检查条件。

合数 $n$ 是 Carmichael 数当且仅当 $\lambda(n)\mid(n-1)$。
等价的 Korselt 判别是：$n$ 无平方因子，且每个素因子 $p$ 都满足 $(p-1)\mid(n-1)$。
素数也满足前一整除式，判定时必须排除素数。
这些合数对所有互素底数都通过 Fermat 判定，不能用单纯的 Fermat 检验代替素性判定。
无平方因子再配合 CRT，也使所有整数 $a$ 满足 $a^n\equiv a\pmod n$。

双版本以小模数的全部实际阶之最小公倍数对拍，另检验大二幂、奇素数幂、
32 位边界及公共指数证书。

UVa10006 完整程序对全部合法输入进行本地 Korselt 对拍；在线 AC 待补。

来源：\url{https://oi-wiki.org/math/number-theory/primitive-root/}；

应用题：\url{https://onlinejudge.org/external/100/10006.pdf}。

\section{Lucas：按素数进制逐位计算}\index{Lucas}
在特征为素数 $p$ 的域中，$(1+x)^p=1+x^p$。
若 $n=\sum_i n_ip^i$，则
\[
(1+x)^n=\prod_i(1+x^{p^i})^{n_i}.
\]
每个因子选择的次数在 $0,\ldots,n_i$ 之间，严格小于 $p$，因此位数表示唯一。
比较 $x^k$ 的系数即得 $\binom nk=\prod_i\binom{n_i}{k_i}\pmod p$；
若某位 $k_i>n_i$，答案立即为零。
仅预处理 $0!,\ldots,(p-1)!$ 及其逆元，这些数均非零。
$p!$ 为零，不能把阶乘表延长后继续用除法公式。
实现见第~\pageref{compact-Lucas}~页。

预处理后每位只做常数次乘法，适合固定小素数模数下的多次查询。
接口接收 $n,k$，不接收“$n,m$ 表示选 $n$ 个”的隐含约定；
P3807 的驱动程序显式调用 $\operatorname{choose}(n+m,n)$。
题目范围内求和安全，完整 64 位输入自行求和时仍要检查溢出。
本地验证包含 Pascal 三角、精确大整数、小模数重建和最大无符号 64 位下标；
P3807 双版本均已通过全部 25 个测试点；动态版记录 297556209，传统版记录 297556596。
完整 64 位参数及额外边界以上述本地对拍验证。
来源：\url{https://oi-wiki.org/math/number-theory/lucas/}。

\section{合数模数组合数与插值}\index{exLucas}\index{Lagrange interpolation}
对素数幂 $q=p^e$，记 $v_p(n!)=\sum_{j\ge1}\lfloor n/p^j\rfloor$，
$U_p(n)=n!/p^{v_p(n!)}$。设
$t=v_p(n!)-v_p(k!)-v_p((n-k)!)$。当 $t\ge e$ 时组合数模 $q$ 为零；否则
\[
\binom nk\equiv U_p(n)\,U_p(k)^{-1}\,U_p(n-k)^{-1}\,p^t\pmod q.
\]
这里仅对与 $p$ 互素的单位部分求逆。各素数幂上的结果通过 CRT 合并，
对应第~\pageref{compact-ExLucas}~页的实现。

在素数域中，$n$ 个模 $p$ 两两不同的横坐标确定唯一的次数小于 $n$ 的多项式。
定义 $w_i=y_i/\prod_{j\ne i}(x_i-x_j)$，则
\[
f(k)=\sum_{i=0}^{n-1}w_i\prod_{j<i}(k-x_j)\prod_{j>i}(k-x_j).
\]
该式不除以 $k-x_i$，查询点命中样本点时仍然成立。
当 $x_i=i$ 时，分母为 $i!\,(n-1-i)!\,(-1)^{n-1-i}$，要求 $n\le p$。
实现见第~\pageref{compact-Lagrange}~页。
对整数幂前缀和 $S_d(k)=\sum_{i=1}^k i^d$，当 $0\le d\le p-2$ 时，
它在模 $p$ 意义下可用次数至多 $d+1$ 的多项式表示；取 $0,1,\ldots,d+1$
的样本即可。不能只因某序列前几项能插值，就断言后续仍由该多项式给出。

\section{阶乘表：组合数与排列数}\index{阶乘表}\index{排列数}
当素数模数为 $p$ 且 $0\le k\le n<p$ 时，阶乘均非零，因此
\[
C(n,k)=n!\,(k!)^{-1}\,((n-k)!)^{-1},\qquad
P(n,k)=n!\,((n-k)!)^{-1}\pmod p.
\]
将表从旧上限扩展到 $N$，先正向补齐阶乘，只对 $N!$ 求一次逆元，
再用 $(i-1)!^{-1}=i\,(i!)^{-1}$ 反向补齐新逆阶乘项即可。
不需要每一项分别快速幂求逆。较小上限的初始化请求保留已有表。
查询前显式扩表，之后查询为常数时间；内部两表重分配时旧元素引用不能继续使用。
实现见第~\pageref{compact-Binomial}~页。

WIDA 的“组合数 (Lucas)”文件实际提供的是普通阶乘、逆阶乘、组合数和排列数，
没有逐位 Lucas 递推。本书以此节覆盖该文件的数学功能，
用显式扩表取代查询时自动扩表，适用范围仍须限制在模数以内。
验证使用独立 Pascal 三角和下降阶乘，另检查百万项表及扩表前后的原查询结果。

\section{组合计数}\index{inclusion-exclusion}\index{Catalan}\index{Stirling numbers}
容斥：$|\bigcup A_i|=\sum_{\emptyset\ne S}(-1)^{|S|+1}|\bigcap_{i\in S}A_i|$。
二项式反演：若 $b_n=\sum_{k=0}^{n}\binom nk a_k$，则
$a_n=\sum_{k=0}^{n}(-1)^{n-k}\binom nk b_k$。
\[
\sum_k\binom ak\binom b{n-k}=\binom{a+b}n,\qquad
C_n=\binom{2n}n-\binom{2n}{n+1}.
\]
差分形式的 Catalan 公式避免直接除以 $n+1$，在模数下尤其有用，但组合数本身仍须正确求模。
第二类 Stirling 数满足
\[
\left\{\begin{matrix}n\\k\end{matrix}\right\}
=\left\{\begin{matrix}n-1\\k-1\end{matrix}\right\}
+k\left\{\begin{matrix}n-1\\k\end{matrix}\right\},
\quad S(0,0)=1.
\]
第一类无符号 Stirling 数把系数 $k$ 换为 $n-1$。
\begin{center}
\begin{tabular}{lll}
\toprule
球与盒 & 空盒允许 & 空盒禁止\\\midrule
不同球，不同盒 & $k^n$ & $k!S(n,k)$\\
相同球，不同盒 & $\binom{n+k-1}{k-1}$ & $\binom{n-1}{k-1}$\\
不同球，相同盒 & $\sum_{j=0}^{k}S(n,j)$ & $S(n,k)$\\
\bottomrule
\end{tabular}
\end{center}
上表默认 $k\ge1$；$n=0$、$k=0$ 等边界应按原问题约定单独处理。

\section{群作用计数}\index{Burnside}\index{Polya}
有限群 $G$ 作用在有限集合 $X$ 上时，轨道数为
\[
|X/G|=\frac1{|G|}\sum_{g\in G}|\operatorname{Fix}(g)|.
\]
对位置置换的 $q$ 色染色，$|\operatorname{Fix}(g)|=q^{c(g)}$，其中 $c(g)$ 为循环数。
环形项链只商去旋转；手链还商去反射，二者群不同，不能共用分母。
模数整除 $|G|$ 时不能使用模逆元，须先在合适的整数模数下求和再做精确除法。

\section{生成函数与递推}\index{generating functions}\index{Berlekamp-Massey}
普通生成函数 $A(x)=\sum a_nx^n$ 的乘法对应普通卷积。
指数生成函数 $A(x)=\sum a_nx^n/n!$ 的乘法对应
$c_n=\sum_k\binom nk a_kb_{n-k}$。
形式幂级数求逆要求常数项可逆；$\log A$ 取 $A(0)=1$，$\exp A$ 取 $A(0)=0$。
有限域中积分涉及 $1/i$，必须避免 $i$ 被特征整除。
\[
B_{\rm new}=B(2-AB)\pmod{x^{2m}},\qquad
(\log A)'=A'/A.
\]
BM 从样本得到最短线性递推。要对未来项使用，必须另外证明序列确实具有不超过相应阶数的线性递推；
少量数值吻合不是证明。矩阵转移、有限状态计数常可提供阶数上界。

\section{线性代数与生成树}\index{rank}\index{Matrix Tree theorem}
域上的线性方程组一致时，解集为一个特解加零空间，零空间维数为未知数个数减秩。
浮点消元中的秩依赖误差阈值；模素数消元是精确域运算；合数模数下不可随意除以非零元。
无向图 Laplacian 的对角元为度数，非对角元为负的边重数。删去同一行列后的行列式为生成树数。
自环不参与生成树；重边需要累加。只有一个顶点时空余子式行列式约定为 1。
有向图需要重新明确入度/出度 Laplacian 与根向内/向外树的方向，不能直接复用无向封装。

\section{模线性方程组：工厂工期}\index{Widget Factory}\index{模高斯消元}
POJ2947（同题 UVa1564）给出每位工人制造的产品类型和起止星期。
设第 $j$ 种产品需 $x_j$ 天，第 $i$ 条记录中出现 $a_{ij}$ 次，
以星期一为 0、星期日为 6，则
\[
\sum_j a_{ij}x_j\equiv \operatorname{finish}_i-\operatorname{start}_i+1\pmod7.
\]
起止两天均计入；同一种产品重复出现必须累加系数。
跨星期无需特判，模数类型负责归一化负数。
每个余数在 $[3,9]$ 中恰有一个代表，因此模方程的解数就是原问题的解数。

直接使用第~\pageref{compact-LinearAlgebra}~页的模高斯消元，模数取 7。
增广矩阵有 $m$ 行、$n+1$ 列，最后一列是右端项。
先判断 \texttt{consistent}：假时输出无解；为真且秩小于 $n$ 时输出多解；
否则读取特解，余数小于 3 的分量加 7。
不能先以秩不足判断多解，因为零系数行仍可能有非零右端项。
本题所有非零系数均可逆；合数模数不具备这一性质，不能直接套用此消元。

完整双版本程序位于 \texttt{verify/poj/2947.*.cpp}。
验证包含所有起止星期组合、产品重复 7 次导致系数归零、矛盾的零行、
独立枚举所有 $3\!\sim\!9$ 天赋值，以及用可逆行变换构造的 300 阶稠密系统。
该应用验证覆盖分类及唯一解输出，不代替零空间基等其他接口的验证。
来源：\url{https://onlinejudge.org/external/15/1564.pdf}；kuangbin 2.10.2。

\section{概率、期望与博弈}\index{expectation}\index{Sprague-Grundy}
期望线性性不要求独立：$\mathbb E[\sum X_i]=\sum\mathbb E[X_i]$。
方差可加需要协方差为零；独立是充分条件。非负整数变量
$\mathbb E[X]=\sum_{k\ge1}\Pr(X\ge k)$。
吸收过程的期望方程 $E_i=1+\sum_jp_{ij}E_j$ 需要检查是否几乎必然吸收及期望是否有限；
不能对闭合非吸收分量直接求唯一有限解。
有限无环公平组合游戏在正常玩法下
\[
\operatorname{SG}(u)=\operatorname{mex}\{\operatorname{SG}(v):u\to v\}.
\]
独立子游戏的 SG 值异或为零当且仅当先手必败。
反常玩法、含环游戏、双方可走步不同的游戏不能直接套这个结论。

\section{数值算法与几何}\index{numerical stability}\index{Pick theorem}
排序必须使用严格弱序；不能以“差小于 eps 则相等”定义通用排序比较器。
叉积与行列式容易发生抵消；整数题优先精确整数判定，浮点构造单独处理误差。
对于简单格点多边形（无孔），Pick 定理 $A=I+B/2-1$，边界格点数
$B=\sum\gcd(|\Delta x|,|\Delta y|)$；不要重复计入边端点。
自适应积分的局部误差估计需要函数足够规则；奇点、间断与强振荡必须先分段处理。
三分搜索要求单峰性，整数域最后枚举剩余区间；二分答案要求判定单调性。

\paragraph{来源索引}
本章为重新组织的公式速查，扩展范围依据 \url{https://oi-wiki.org/math/}。
小模数 exLucas 与有限域拉格朗日插值已有双风格实现和本地验证；素数模平方根已有 Cipolla 实现；仍需补齐二次剩余的其他方法与定理、Min25、Newton 插值、子集卷积、行列式进阶、数值积分等内容及验证。本章公式不能代替这些待完成代码。

\section{线性递推：系数方向与外推条件}
\index{线性递推}
约定 $a_i=\sum_{j=1}^{k}c_{j-1}a_{i-j}$，初值是
$a_0,\ldots,a_{k-1}$。对应特征多项式为
\[
F(x)=x^k-c_0x^{k-1}-c_1x^{k-2}-\cdots-c_{k-1}.
\]
若 $x^n\bmod F(x)=\sum_{i=0}^{k-1}r_ix^i$，则
$a_n=\sum_{i=0}^{k-1}r_ia_i$。代码由高次向低次消去多项式项，
第 $j$ 个系数写入原次数减 $j$ 的位置，避免倒置系数。
例如 Fibonacci 初值为 $[0,1]$，递推系数为 $[1,1]$。

BM 在域上求有限前缀的最短递推。本书实现要求素数模数；
一般的模合数环不能保证非零偏差存在逆元。
仅凭若干样本无法证明真实序列以后一直遵循同一个规律。
若事先知道序列满足阶数至多 $d$ 的齐次线性递推，
至少给出前 $2d$ 项，再用恢复出的系数求后续项。
系数向量末尾的零不能擅自删除：它可能对应非平稳的初始段，
改变阶数会改变递推开始适用的位置。

\clearpage
\section{素数模高次剩余与单位根}
实现见第~\pageref{compact-KthResidue}~页；依赖本书离散对数、线性同余和快速幂。
设 $p$ 为素数，$g$ 是原根，$k>0$。
$a=0$ 时根只有零；否则写 $a=g^b$、$x=g^y$，方程转为
\[
ky\equiv b\pmod{p-1}.
\]
令 $d=\gcd(k,p-1)$。当 $d\nmid b$ 时无解；否则指数解为
$y=y_0+i(p-1)/d$，$0\le i<d$。
因此用三个数存全部根：
\[
\texttt{first}=g^{y_0},\qquad
\texttt{ratio}=g^{(p-1)/d},\qquad \texttt{count}=d.
\]
依次乘 ratio 即可枚举；不保证数值递增，也不保证 first 是最小根。
ratio 是阶为 $d$ 的单位根，因而枚举恰有 $d$ 个不同的根。
模 $p=2$ 同样有效；指数同余的模数是 1，唯一非零根为 1。

接口支持 $p\le10^{12}$，需要调用者提供已验证的素数与原根；
$k$ 支持完整 unsigned long long，先对 $p-1$ 取模后解同余。
$a$ 支持完整 signed long long，先正规化。
返回空 optional 表示无解，返回 \texttt{\{0,1,1\}} 表示唯一零根。
给定原根后的期望时间、空间为 $O(\sqrt p)$；枚举全部根另外需要 $O(d)$ 时间。
模乘须先扩展到 128 位，不可用 64 位乘积直接取模。

测试穷举小于 100 的每个素数、全部原根及小指数，
用任意精度模幂逐个枚举候选根并比较整个集合；
覆盖负数、64 位边界、模 2、无解与零根。
另检查 998244352 个根的紧凑表示，以及素数 999999999989 上的植根测试；
后一个素数与原根的条件由试除及乘法阶证书独立确认。
普通与 ASan/UBSan 均通过，尚无专门在线 AC。

这是 \href{https://oi-wiki.org/math/number-theory/residue/}{OI Wiki 高次剩余} 中
离散对数转线性同余方法的素数模实现。
素数幂及非互素被开方数见下一节；一般合数模数见后文；改良 Tonelli--Shanks 尚需另补，
不能把本节状态解读为该页面全部覆盖。

\clearpage
\section{素数幂模高次剩余与根的提升}
实现见第~\pageref{compact-PrimePowerRoots}~页。
令 $M=p^e$。当 $a=0$ 时，全部根是 $p^{\lceil e/k\rceil}$ 的倍数。
否则令 $t=v_p(a)<e$，必须有 $k\mid t$，否则无解。
取 $s=t/k$，$q=p^{e-t}$，写 $x=p^s y$，只需求单位方程
\[
y^k\equiv a/p^t\pmod q.
\]
每个单位根 $y$ 对应原模数下的根
\[
x=p^s y+\ell p^s q,\qquad 0\le\ell<p^{t-s}.
\]
这些提升保持互不相同，也解释了非互素被开方数可能产生大量根。

奇素数时，提供模 $p^e$ 的原根，约化后仍是模 $q$ 的原根，
沿用离散对数转线性同余的方法。
$q=2$ 时单位根只有 1；$q\ge4$ 且为二的幂时，单位表示为
$(-1)^z5^y$。符号指数模 2，5 的指数模 $q/4$；
$q=4$ 时只有符号分支。分别解两个指数同余即可，不可假设二的高次幂存在原根。

返回结构体保存 first、两个 ratio、两个 count，以及 step、lifts。
全部根由下式给出：
\[
\bigl(\texttt{first}\cdot\texttt{ratio}[0]^i\cdot\texttt{ratio}[1]^j\bmod\texttt{step}\bigr)
+\ell\cdot\texttt{step}.
\]
其中 $0\le i<\texttt{count}[0]$、$0\le j<\texttt{count}[1]$、
$0\le\ell<\texttt{lifts}$。
\texttt{get} 已处理模乘溢出，\texttt{size} 返回三者乘积；
不保证根按数值排序。零根情形复用同一表示。

模数上限 $10^{12}$，$k$ 支持完整无符号 64 位。
只检查范围，不检查 $p$ 的素性和奇素数原根的阶，调用者须保证。
默认参数 $g=0$ 仅用于 $p=2$；奇素数不能漏传原根。
时间和空间由 BSGS 决定，期望 $O(\sqrt q)$；
单次按索引取根使用快速幂，需 $O(\log M)$。

已穷举多个素数的全部不超过 256 的素数幂、所有被开方数与小指数，
用任意精度模幂直接枚举所有根，比较整个集合。
另测有符号与无符号 64 位边界、$2^{39}$ 的非平凡离散对数、
$3^{20}$ 的非互素方程，以及数千亿根的紧凑表示。
普通与 ASan/UBSan 均通过；尚无专门在线 AC。
一般合数的 CRT 组合见下一节；改良 Tonelli--Shanks 仍未完成，OI Wiki 页面继续标记部分覆盖。

\clearpage
\section{一般合数模高次剩余：CRT 组合}
实现见第~\pageref{compact-CompositeRoots}~页。
调用者提供完整分解 $M=\prod_{t=0}^{F-1}m_t$，其中 $m_t=p_t^{e_t}$ 两两互素，
并提供每个奇素数幂的原根。先分别求局部根族；任一局部无解，整体即无解。
若局部根数为 $c_t$，则整体根数为 $\prod_t c_t$。
只保存局部根族，不创建这个大小的数组。

对每个因子预处理
\[
E_t=\frac M{m_t}\left(\left(\frac M{m_t}\right)^{-1}\bmod m_t\right)\bmod M.
\]
对一组局部根 $r_t$，唯一对应的标准代表是
\[
x=\sum_t r_tE_t\bmod M.
\]
逆元存在是因为因子两两互素。
\texttt{get(index)} 将索引按各个 $c_t$ 转成混合进制数字，
再把局部索引拆成两个乘法根族索引和加法提升索引，最后用上述公式组合。
CRT 双射保证不重复、不遗漏，输出顺序不保证递增。

$M\le10^{12}$，$k>0$；空因子列表表示 $M=1$，返回唯一代表 0。
\texttt{mod} 保存模数，\texttt{total} 保存总解数，
\texttt{parts} 和 \texttt{weight} 保存局部根族与 $E_t$。
模数乘积检查上界，CRT 的乘积先扩展至 128 位。
单次按索引查询为 $O(F\log M)$；求解的主要时间和空间为局部 BSGS，
因子列表接口不包含整数分解与原根搜索的成本；自动准备重载见下一节。

已对 $1\le M\le180$、所有被开方数与小指数直接枚举所有根，
与两套代码的完整输出集合比较，并反转因子顺序重新检查。
另覆盖有符号与无符号 64 位边界、混合素数幂上的非互素方程和大量零根，
以及十一种不同素因子组成模数时的全部 1024 个平方根。
普通与 ASan/UBSan 均通过；尚无专门在线 AC。
至此已有已知分解及原根时的 BSGS 求根路线，
更快的改良 Tonelli--Shanks 等内容仍未覆盖，OI Wiki 条目保留部分完成状态。

\clearpage
\section{自动准备模数分解与原根}
实现见第~\pageref{compact-root_factors}~页。
\texttt{root\_factors(M,rho)} 把 $M\le10^{12}$ 分解为互异素数的幂，
返回合数求根需要的 $(p,e,g)$ 列表；模数 1 返回空列表。
二的幂不需要原根，固定填零。
奇素数幂 $m=p^e$ 的乘法群是循环群，设 $\varphi=m/p\cdot(p-1)$。
分解 $\varphi$ 后，候选 $g$ 为原根当且仅当
\[
p\nmid g,\qquad g^{\varphi/q}\not\equiv1\pmod m
\quad\text{对每个不同素因子 }q\mid\varphi.
\]
从 2 起枚举候选，返回最小原根；所有奇素数幂均有原根。
这里检查的是模 $p^e$ 的阶，不能仅取一个模 $p$ 的原根而不验证提升。

重载 \texttt{CompositeRoots::solve(a,k,M,rho)} 自动调用此准备过程。
传统版为 \texttt{Composite\_Roots::Solve} 和 \texttt{Root\_Factors}，
随机分解工作区类型为 \texttt{Pollard\_Rho}。
如果需要在同一个模数下处理多个方程，先保存因子列表，调用原有列表接口，
避免重复分解和原根搜索。
准备成本包括 $M$ 与各 $\varphi$ 的因数分解，以及逐个候选的模幂检验；
不能把这部分直接算作常数时间。

独立试除验证 $1\ldots500$ 的全部模数与多组接近 $10^{12}$ 的边界值，
并用任意精度模幂验证返回原根的阶。
小模数进一步检查更小候选不能生成全部单位。
直接传模数的两套求根接口，在小模数上与全部候选根的枚举结果比较。
普通与 ASan/UBSan 均通过；尚无自动准备入口的在线 AC。

\clearpage
\section{带权矩阵树定理与方向约定}
实现见第~\pageref{compact-MatrixTree}~页。
返回的是每棵树的边权乘积，再对所有树求和，并对素数取模。
无权计数只需把所有边权设为 1。
重边是不同选择，自环不属于任何生成树；单点图的空树乘积为 1。
零权和负权都按域上的运算处理，因此结果为零可能来自抵消，不能作为连通性判定。

无向边 $uv$ 权值 $w$ 对拉普拉斯矩阵贡献为
$L_{uu},L_{vv}$ 各加 $w$，$L_{uv},L_{vu}$ 各减 $w$。
删除 root 行和列后求行列式，与 root 的选择无关。
对于每个点都能沿有向边走到根的树形图，
边 $u\to v$ 贡献 $L_{uu}\mathrel{+}=w$、$L_{uv}\mathrel{-}=w$；
删除根行列后得到内向树形图的权值和。
外向树形图先反转所有边，再使用同一构造。

例如只有一条边 $0\to1$ 时，内向根 1 的答案为该边权，内向根 0 为零；
外向根 0 的答案为该边权。
\href{https://www.luogu.com.cn/problem/P6178}{P6178} 指定的是以 1 为根的外向树，
驱动把点号减一，并选择 \texttt{away\_from\_root}。
只直接构造 $(n-1)\times(n-1)$ 余子式，时间 $O(n^3+m)$、额外空间 $O(n^2)$。
行列式复用本书的素数模高斯消元，不支持将 mod 随意改成合数。

测试独立枚举 $n-1$ 条边的所有选择，检查连通性或根向约束，
用任意精度整数计算边权乘积之和，再分别对多个素数取模。
两套实现覆盖所有根、三种方向、自环重边、负权及 64 位权值边界，
并通过 220 点完全图的加权 Cayley 公式检查。
P6178 完整驱动另测 300 点、十万条边以及输入输出约定。
普通与 ASan/UBSan 均通过。
两套代码已在 P6178 通过全部 11 个测试点：
\href{https://www.luogu.com.cn/record/297619886}{动态版记录 297619886}、
\href{https://www.luogu.com.cn/record/297620263}{传统版记录 297620263}。
单点最大用时分别为 64 ms、62 ms，内存峰值为 2.85 MB、2.40 MB。
在线测试覆盖无向与根为 1 的外向树；其他根、内向树和负权仍使用本地证据。
定理背景可参考 \href{https://oi-wiki.org/graph/matrix-tree/}{OI Wiki 矩阵树定理}。

\clearpage
\section{任意正模数行列式}
实现见第~\pageref{compact-determinant_mod}~页。
素数模高斯消元可以除以任意非零主元，合数模数没有这个性质。
本节只使用倍加行和交换行，不求逆元。
先把元素正规化到 $[0,m)$，对第 $i$ 列的两个元素 $a,b$，
用整数商 $q=\lfloor a/b\rfloor$ 执行
$R_i\leftarrow R_i-qR_j$，再交换 $R_i,R_j$。
该列的新数对为 $(b,a\bmod b)$，所以循环按欧几里得算法终止。
其余元素每步对 $m$ 取模，只改变整数代表，不改变行列式模 $m$ 的值。

倍加行不改变行列式；交换行改变符号。
消去所有下三角元素后，乘上对角元素及累计符号即得结果。
剩余部分的该列全零时直接返回零，不需要判断某个非零元素是否与模数互素。
例如 $\operatorname{diag}(2,3)$ 在模 8 下的行列式为 6，
不能因为第一个主元没有逆元就判定失败。

支持任意正 signed 64 位模数与完整 signed 64 位输入元素。
正规化后单个乘积小于 $2^{126}$，行更新差值和结果乘积都放入有符号 128 位。
空矩阵按约定返回 $1\bmod m$；模 1 的所有返回值为零。
时间上界 $O(n^3\log m)$，矩阵副本占 $O(n^2)$ 空间。
本节可以用于合数模的拉普拉斯余子式，但之前的 \texttt{MatrixTree<mod>}
接口仍使用素数模高斯，不能仅把其模板参数改成合数；合数模使用下一节的独立接口。

测试用排列定义穷举所有元素属于 $\{-1,0,1\}$ 的三阶矩阵，
并用任意精度 Bareiss 消元验证随机矩阵。
覆盖不可逆主元、奇异矩阵、模 1、有符号 64 位边界、Fibonacci 长欧几里得过程，
以及 300 阶对角矩阵和完全图余子式的 Cayley 公式。
两套版本普通与 ASan/UBSan 均通过；尚无专门在线 AC。
行变换依据可参考 \href{https://oi-wiki.org/math/linear-algebra/determinant/}{OI Wiki 行列式}。

\clearpage
\section{任意正模数的矩阵树计数}
实现见第~\pageref{compact-MatrixTreeMod}~页。
拉普拉斯余子式表示生成树权值和的恒等式本身不要求模数为素数；
需要换掉的是依赖模逆元的行列式算法。
本节沿用无向、内向与外向三种构造，但使用欧几里得行列式消元。
输入边权和正模数均支持 signed 64 位，接口名为
\texttt{MatrixTreeMod::count}，传统版为 \texttt{Matrix\_Tree\_Mod::Count}。

先把边权正规化到 $[0,m)$，每次矩阵元素加减仍先扩展至 128 位，
再取模存回 64 位。重边的权值和可能超过 64 位范围，不能先累加到普通整数中。
只构造删除根后的余子式，并将其存储直接交给行列式函数。
单点返回 $1\bmod m$，模 1 返回零。
负权与零权仍按代数权值处理，返回零不能直接解释为没有生成树。

独立参考程序逐个枚举边集，用任意精度整数计算权值和。
已检查多个合数模数、所有根与三种方向、断图、重边自环及 64 位极值权重；
另以大权值完全图的 Cayley 公式验证 160 点规模。
普通与 ASan/UBSan 均通过，尚无该接口的在线 AC。
此前 P6178 的 AC 属于素数模版本，不自动转移到本接口。

\clearpage
\section{斯特林数的快速整行计算}
第二类实现见第~\pageref{compact-stirling_second_row}~页，
第一类实现见第~\pageref{compact-stirling_first_row}~页。
本节默认模数 $p=998244353$，允许用模板参数指定素数与原根，复用参数化 NTT。
第二类 $S(n,k)$ 计数无标号非空集合划分；第一类 $c(n,k)$ 计数排列的循环数。
两者不能互换，第一类本节返回无符号版本。

第二类的容斥公式为
\[
S(n,k)=\sum_{i=0}^{k}\frac{i^n}{i!}\frac{(-1)^{k-i}}{(k-i)!}.
\]
分别构造 $a_i=i^n/i!$ 和 $b_i=(-1)^i/i!$，一次卷积取前 $m+1$ 项即可。
$n$ 支持无符号 64 位；$i>0$ 时指数对 $p-1$ 取模后快速幂，
$i=0$ 时单独区分 $n=0$ 和 $n>0$，不可直接套用费马降幂。
$S(0,0)=1$，正 $n$ 的 $S(n,0)=0$，$k>n$ 时为零。
令 $L$ 为 NTT 长度上限，要求 $2m+1\le L$；默认模数下即 $m<2^{22}$。

无符号第一类是升阶乘多项式的系数：
\[
x(x+1)\cdots(x+n-1)=\sum_{k=0}^{n}c(n,k)x^k.
\]
令 $F_m(x)=x(x+1)\cdots(x+m-1)$，倍增式为
$F_{2m}(x)=F_m(x)F_m(x+m)$；奇数长度再乘 $x+2m$。
平移系数满足 $[x^i]F_m(x+m)=\sum_{j=i}^m f_j j!m^{j-i}/(i!(j-i)!)$，
将 $f_jj!$ 倒序后与 $m^j/j!$ 卷积即可。
每层只递归计算一次半长多项式，总时间 $O(n\log n)$，空间 $O(n)$。
要求 $n+1\le L$，默认模数下即 $n<2^{23}$；$n=0$ 返回常数多项式 1。
如果需要下降阶乘对应的带符号数，将第 $k$ 项乘 $(-1)^{n-k}$。

验证使用小规模集合划分、排列循环的直接枚举，以及 300 行的递推表；
大指数第二类另用独立转移矩阵快速幂验证。
规模检查包含二十万项第二类行与两万项第一类行，
用幂函数的下降阶乘展开和升阶乘求值恒等式检查。
普通与 ASan/UBSan 均通过。
\href{https://www.luogu.com.cn/problem/P5395}{P5395} 和
\href{https://www.luogu.com.cn/problem/P5408}{P5408} 使用模 167772161，
现已支持指定模板参数 167772161。两题均获两套码风在线 AC、各 10 个测试点全过：
第二类\href{https://www.luogu.com.cn/record/297626422}{动态版}与
\href{https://www.luogu.com.cn/record/297626660}{传统版}，
第一类\href{https://www.luogu.com.cn/record/297627360}{动态版}与
\href{https://www.luogu.com.cn/record/297627676}{传统版}。
OJ 覆盖题面模数与行长，其他模数和第二类完整无符号指数仍由本地证据支持。
该模数已测试二十万项第二类行与 262143 阶第一类行，普通与消毒器均通过。
按列计算及 OI Wiki 的其他斯特林数应用仍待补，
\href{https://oi-wiki.org/math/combinatorics/stirling/}{对应条目}保留部分覆盖状态。

\clearpage
\section{参数化 NTT 与模数适配}
实现见第~\pageref{compact-NttConvolution}~页。
传入素数 $p$ 和原根 $g$；长度 $n$ 必须为二次幂且整除 $p-1$。
代码自动计算 $L=2^{v_2(p-1)}$ 作为长度上限，不代表分配这个长度的数组没有成本。
两个非空数组的普通卷积需要补齐到不小于长度和减一的二次幂。
模 998244353 的 $L=2^{23}$，模 167772161 的 $L=2^{25}$，两者均可用原根 3。
不能只改模数而忽略原根和长度条件；接口只检查基本范围，不检查素性或原根的阶。

正变换按 $g$ 的相应单位根计算，逆变换使用逆单位根并乘 $n^{-1}$。
模数支持 int 范围内的素数，模整数的加减和乘积使用 64 位中间量。
蝶形层以半长度递增，避免最后一层之后的有符号整数溢出。

独立参考用任意精度整数直接计算 DFT 和朴素卷积，
同时验证正逆变换复原。已检查七组素数和原根，包括模 2、3、17、97，
斯特林数的默认模数与洛谷题目模数，以及 2013265921、原根 31。
这些测试中的素性及原根条件另用试除和乘法阶证书确认。
大规模全一数组卷积逐项比较已知三角形系数，检查补齐长度的边界。
两套实现普通与 ASan/UBSan 均通过。


\clearpage
\section{Bostan--Mori 与快速递推单项}
代码见第~\pageref{compact-BostanMori}~页。
对于 $a_i=\sum_{j=1}^k c_{j-1}a_{i-j}$，令
$Q(x)=1-\sum_{j=1}^k c_{j-1}x^j$，
$P(x)=(Q(x)\sum_{i=0}^{k-1}a_ix^i)\bmod x^k$，则 $[x^n]P/Q=a_n$。
注意系数按前一项、前两项的顺序给出，初值从 $a_0$ 开始。

\[
 \frac{P(x)}{Q(x)}=\frac{P(x)Q(-x)}{Q(x)Q(-x)}
 =\frac{U_0(x^2)+xU_1(x^2)}{V(x^2)}.
\]
分母的奇数次项抵消，因此只保留分子与 $n$ 同奇偶的项，
分母保留偶数项，再将 $n$ 右移一位。
最终 $n=0$ 时答案是 $P(0)/Q(0)$。
实现不需要分子次数小于分母次数，空分子直接返回零；分母常数项必须非零。
每轮系数长度不增加，初始最大长度 $d$ 必须满足 $2d-1$ 不超过 NTT 容量。
总时间 $O(d\log d\log(n+1))$，空间 $O(d)$；$n=0$ 直接取常数项。

独立验证包括按定义做形式幂级数除法、友矩阵快速幂，以及
$(1-x)^{-32000}$ 的二项式系数闭式；覆盖完整无符号 64 位下标、
常数分母、不等于 1 的非零常数项、空分子、不真分式、零递推系数和多种 NTT 素数。
\href{https://www.luogu.com.cn/problem/P4723}{P4723} 驱动读取有符号系数并归一化；
两套版本均获在线 AC：
\href{https://www.luogu.com.cn/record/297628790}{动态版}与
\href{https://www.luogu.com.cn/record/297629192}{传统版}各 5 个测试点全部通过，
最慢单点分别为 907 ms、905 ms。OJ 覆盖模 998244353、$n\le10^9$、$k\le32000$；
其他模数、完整 64 位下标与通用有理函数接口的扩展范围由本地测试支持。
\href{https://oi-wiki.org/math/poly/linear-recurrence/}{OI Wiki 线性递推}
中的连续区间取项与其他快速算法仍待补，不将单项算法视为整页覆盖。

\clearpage
\section{集合变换与不相交子集卷积}
代码见第~\pageref{compact-SetConvolution}~页与
第~\pageref{compact-subset_convolution}~页。
长度 $N=2^m$ 确定全集的位数，数组第 $s$ 项对应集合 $s$。
OR 变换计算 $\widehat a_s=\sum_{t\subseteq s}a_t$，
AND 变换计算所有超集的和；逆变换将逐位加法改成减法。
XOR 变换的系数是 $(-1)^{\operatorname{popcount}(s\mathbin{\&}t)}$，
蝶形将 $(u,v)$ 变为 $(u+v,u-v)$，逆变换每层再除以 2。
因此 OR、AND 可用于任意模数，XOR 需要 2 可逆；本实现限制为奇数模数，
使用 $(\mathrm{mod}+1)/2$，不对合数模数使用费马小定理。
三类卷积均是变换后逐项相乘，再逆变换。

\[
 c_s=\sum_{t\subseteq s}a_t b_{s\setminus t}
 \qquad\text{要求两个集合不相交。}
\]
普通 OR 卷积允许重叠，不能直接用于上式。
将输入按集合大小分层，$A_{i,s}=[|s|=i]a_s$，$B$ 同理。
每层 OR 变换后，对层数做卷积：
$H_{k,s}=\sum_{i=0}^k\widehat A_{i,s}\widehat B_{k-i,s}$。
对每个 $k$ 逆 OR 变换，得到并集恰为 $s$、两集合大小之和为 $k$ 的贡献。
取 $k=|s|$，由 $|u|+|v|=|u\cup v|+|u\cap v|$ 得到交集为空。
时间 $O(m^22^m)$，空间 $O(m2^m)$；本实现逐层复用结果缓冲区，
不用第三组二维数组。$m=20$ 时两组分层数组本身占 168 MiB，另计输入和线性缓冲。

验证独立枚举变换系数、所有下标对与子掩码，并检查复合模数与
\texttt{INT\_MAX} 模数。
大规模使用 $a_s=x^{|s|}$、$b_s=y^{|s|}$ 的逐位乘积恒等式：
OR 为 $(x+y+xy)^{|s|}$，AND 为 $(xy)^{|s|}(1+x+y)^{m-|s|}$，
XOR 为 $(x+y)^{|s|}(1+xy)^{m-|s|}$，不相交子集卷积为 $(x+y)^{|s|}$。
\href{https://www.luogu.com.cn/problem/P4717}{P4717} 两套版本各 10 点在线 AC：
\href{https://www.luogu.com.cn/record/297630491}{动态版}和
\href{https://www.luogu.com.cn/record/297630911}{传统版}，最慢单点 84 ms、81 ms。

\href{https://www.luogu.com.cn/problem/P6097}{P6097} 两套版本各 5 点在线 AC：
\href{https://www.luogu.com.cn/record/297631186}{动态版}和
\href{https://www.luogu.com.cn/record/297631585}{传统版}，最大测试点显示 2.70 s、2.69 s，
内存 188.83 MB、189.00 MB。两题 OJ 仅验证各自题面模数和长度范围，
其他模数及长度 1 仍由本地独立测试覆盖。
\href{https://oi-wiki.org/math/poly/fwt/}{OI Wiki FWT} 的其他运算及更广泛应用仍待覆盖。

\clearpage
\section{Taylor 平移与等比点求值}
代码见第~\pageref{compact-polynomial_shift}~页和
第~\pageref{compact-chirp_z}~页。
多项式使用升幂系数，输入长度 $n$ 表示次数小于 $n$，允许末尾零。
平移输出的第 $i$ 项满足
\[
 [x^i]f(x+c)=\frac1{i!}\sum_{j=i}^{n-1} f_jj!\frac{c^{j-i}}{(j-i)!}.
\]
倒序 $f_jj!$，与 $c^j/j!$ 卷积，再取第 $n-1-i$ 项并乘 $1/i!$。
保留原数组长度；零多项式可以用空数组表示，返回空数组。
使用 NTT 素数及原根，要求 $2n-1$ 不超过变换容量；此时所需阶乘非零。
时间 $O(n\log n)$、空间 $O(n)$。
这与给出 $f(0),f(1),\ldots$ 后平移点值的任务不同，后者尚待补充。

Chirp Z 返回 $f(ar^i)$，$0\le i<m$。
设 $T_j=j(j-1)/2$，由 $ij=T_{i+j}-T_i-T_j$ 得
\[
 f(ar^i)=r^{-T_i}\sum_{j=0}^{n-1}
        f_ja^jr^{-T_j}r^{T_{i+j}}.
\]
将第一项数组倒序，与 $r^{T_j}$ 卷积，抽取第 $n-1+i$ 项。
三角数幂通过累乘生成，不计算可能溢出的 $j(j-1)$。
令 $L$ 为不小于 $n+m-1$ 的最小二次幂，直接做模 $x^L-1$ 的循环卷积。
普通乘积最高次数为 $2n+m-3$，而所需区间任意下标 $t\ge n-1$
满足 $t+L\ge2n+m-2$，因此不会收到高次项回卷，所取系数仍然精确。
要求 $n+m-1$ 不超过 NTT 容量，总时间 $O((n+m)\log(n+m))$、空间 $O(n+m)$。

约定 $r^0=1$，故 $r=0$ 时第一项为 $f(a)$，余项均为 $f(0)$；
$a=0$ 时所有点为零，$r=1$ 时所有点相同。这些情况单独计算，避免对零求逆。
$m=0$ 返回空数组，空输入返回 $m$ 个零。
同一份算法支持小阶公比、重复点与有限域中多次绕回，输出保持原顺序。

独立验证采用 Pascal 二项式展开和 Horner 求值，涵盖 7 种 NTT 素数、
零公比与单位公比、小素数完整容量边界。
大规模平移用 $(x+b)^d$ 检查每个系数；等比点求值用几何级数闭式逐点检查。
普通与 ASan/UBSan 均通过。
Library Checker 的
\href{https://judge.yosupo.jp/problem/polynomial_taylor_shift}{Polynomial Taylor Shift} 和
\href{https://judge.yosupo.jp/problem/multipoint_evaluation_on_geometric_sequence}{等比点求值}
已准备两套完整驱动；本地检查各题 $524288$ 的最大规模，尚无在线 AC。
参考\href{https://oi-wiki.org/math/poly/shift/}{OI Wiki 平移}及
\href{https://oi-wiki.org/math/poly/czt/}{Chirp Z}，更广泛的点值平移和应用另行覆盖。

\clearpage
\section{可持久化区间拼接的懒标记}
代码见第~\pageref{compact-PersistentRange}~页。
对象从全零版本开始，同一对象内版本共享坐标域。
区间加仅复制被访问节点，不向旧子节点下传标记。
节点的和满足
$\mathrm{sum}=\mathrm{left.sum}+\mathrm{right.sum}+\mathrm{lazy}\cdot\mathrm{len}$。
读取时把祖先累积增量作为参数传下去，因此查询完全只读。

拼接含义为将目标版本某个半开区间替换成源版本同一坐标区间。
由于两棵树祖先的 lazy 可能不同，不能直接将源子树挂到目标祖先下面。
递归参数 offset 保持“源祖先增量减目标祖先增量”；下降时更新为
$\mathrm{offset}+\mathrm{source.lazy}-\mathrm{destination.lazy}$。
当区间完整覆盖，复制源节点并将这个 offset 加到其 lazy 和 sum，
此后挂到目标祖先下面时，逻辑值才与源版本相同。
offset 为零时直接共享源节点；没有覆盖时保留目标节点。
空区间、零增量与全区间拼接都可仅增加版本别名。

每一层至多有两个边界节点，其他分支直接返回或复制一个整段节点，
所以单次修改、拼接均为 $O(\log n)$ 新节点，查询为 $O(\log n)$ 时间、零新节点。
不支持不同坐标偏移的平移粘贴，也不回收仍被旧版本引用的节点。
所有 long long 的和、差、懒标记与长度乘积都应在范围内；
节点数与版本数须小于 \texttt{INT\_MAX}。

验证用独立向量保存各分支版本，逐区间检查更新、拼接和历史版本；
包含正负增量、不同祖先标记、空区间、全段共享，以及查询前后的节点数量检查。
规模验证包含长度 200000 的 10000 次区间版本组合，
并在长度 \texttt{INT\_MAX} 的隐式坐标域验证端点操作与共享根。
普通与 ASan/UBSan 均通过；尚无该新接口的在线 AC。
来源需求是\href{https://icpcarchive.github.io/Asia\%20East\%20Continent\%20Contests/Hangzhou/2023\%20Asia\%20Hangzhou\%20Regional\%20Contest/solution.pdf}{2023 杭州站题解}
第 24--26 页的 K 题，完整题目驱动尚待核验。
